import { useLogin } from '@/app/api/useApi';

const useAuth = () => {
  return { user: 'test' };
};

export default useAuth;
//GUARDA LA GUIDA AL LINK https://blog.logrocket.com/complete-guide-authentication-with-react-router-v6/
